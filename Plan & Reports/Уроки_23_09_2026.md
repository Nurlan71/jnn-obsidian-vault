# Уроки сессии 23.09.2026

## Whisper / Транскрибация
- Скилл `whisper-transcribe`: `language="ru"` обязательно (иначе LLM отвечает на английском).
- Автоматизация: вебхук (`port 8443`) предпочтительнее крон (Нурлан: «кто будет ждать 5 мин?»).
- Не заменяет основной LLM (`inkling`/`minimax`).

## SSL / Сайт travel.jnnn8n.ru
- Безопасный метод: самоподписанный сертификат (`/etc/ssl/travel/`), т.к. Certbot заблокирован `docker-proxy` (порт 80).
- Контейнер nginx (`evolution_chatwoot-nginx`) — работает, но сайт (`curl` = 000) требует настройки прокси в контейнере.

## Google Drive
- Дубли: `JNN — Производство` (5 копий в корне), `Главная.xlsx` (корень + папка).
- Не удалять без сверки содержимого (`rclone lsf` + `openpyxl`).

## Память
- Бюджет ограничен (~10K / 2.2K). При переполнении заменять устаревшие записи актуальными уроками.
- Не плодить лишние навыки — готовые блоки в ЛС.
